{
  "name": "sales-copilot-client",
  "version": "0.1.0",
  "private": true,
  "description": "Sales Copilot desktop client — audio capture + overlay (Stage 1).",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && mkdir -p dist/renderer && cp src/renderer/*.html dist/renderer/",
    "start": "npm run build && electron .",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "syscapture": "file:./native/syscapture"
  },
  "devDependencies": {
    "@electron/rebuild": "^3.7.2",
    "electron": "^33.0.0",
    "node-addon-api": "^8.8.0",
    "typescript": "^5.6.0"
  }
}
